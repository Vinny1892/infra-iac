# Dynamic values — generated by: deploy.sh generate-values
server:
  service:
    type: ClusterIP

  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: PLACEHOLDER_ARGOCD_ROLE_ARN

configs:
  params:
    server.insecure: true
